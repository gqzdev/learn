<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xafdy.dao.CourseFileMapper">
  <resultMap id="BaseResultMap" type="com.xafdy.model.CourseFile">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Mar 29 17:40:13 CST 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="path" jdbcType="VARCHAR" property="path" />
    <result column="course_id" jdbcType="INTEGER" property="courseId" />
    <result column="record_time" jdbcType="TIMESTAMP" property="recordTime" />
    <result column="teacher_id" jdbcType="INTEGER" property="teacherId" />
  </resultMap>
  
  <insert id="saveEntity" parameterType="com.xafdy.model.CourseFile">
  	 INSERT into course_file (path, course_id, record_time, teacher_id) VALUES (#{path}, #{courseId}, #{recordTime}, #{teacherId})
  </insert>
  
  <select id="searchEntity" parameterType="int" resultMap="BaseResultMap">
  	SELECT v.id, v.path, (SELECT name FROM course WHERE id = v.course_id) AS courseName, v.record_time FROM course_file AS v WHERE teacher_id = #{id}
  </select>
  
  <select id="searchAllEntity" resultMap="BaseResultMap">
  	SELECT v.id, v.path, (SELECT name FROM course WHERE id = v.course_id) AS courseName, v.record_time FROM course_file AS v
  </select>
  
  <select id="searchEntityById" resultMap="BaseResultMap" parameterType="int">
  	SELECT * FROM course_file WHERE id = #{id}
  </select>
  
  <delete id="deleteEntity" parameterType="int">
  	DELETE FROM course_file WHERE id = #{id}
  </delete>
  
</mapper>