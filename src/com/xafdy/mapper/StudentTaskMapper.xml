<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xafdy.dao.StudentTaskMapper">
  <resultMap id="BaseResultMap" type="com.xafdy.model.StudentTask">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sat Mar 31 20:18:35 CST 2018.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="student_id" jdbcType="INTEGER" property="studentId" />
    <result column="task_id" jdbcType="INTEGER" property="taskId" />
    <result column="score" jdbcType="INTEGER" property="score" />
    <result column="record_time" jdbcType="TIMESTAMP" property="recordTime" />
  </resultMap>
  
  <insert id="saveEntity" parameterType="com.xafdy.model.Student">
  	 INSERT into student_task (student_id, task_id, score, record_time) VALUES (#{studentId}, #{taskId}, #{score}, #{recordTime})
  </insert>
  
  <select id="searchEntity" resultMap="BaseResultMap">
  	SELECT t.id, t.record_time, t.score, (SELECT name FROM student WHERE id = t.student_id) AS studentName, (SELECT c.name FROM task AS t1 JOIN course AS c ON t1.course_id = c.id WHERE t1.id = t.task_id) AS taskName FROM student_task AS t
  </select>
  
  <select id="searchEntityByStuId" parameterType="int" resultMap="BaseResultMap">
  	SELECT t.id, t.record_time, t.score, (SELECT name FROM student WHERE id = t.student_id) AS studentName, (SELECT c.name FROM task AS t1 JOIN course AS c ON t1.course_id = c.id WHERE t1.id = t.task_id) AS taskName FROM student_task AS t WHERE t.student_id = #{stuId}
  </select>
  
</mapper>