<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xafdy.dao.TaskMapper">
	<resultMap id="BaseResultMap" type="com.xafdy.model.Task">
		<!-- WARNING - @mbg.generated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Thu Mar 29 
			21:41:54 CST 2018. -->
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="teacher_id" jdbcType="INTEGER" property="teacherId" />
		<result column="course_id" jdbcType="INTEGER" property="courseId" />
		<result column="record_time" jdbcType="TIMESTAMP" property="recordTime" />
	</resultMap>

	<insert id="saveEntity" parameterType="com.xafdy.model.Task">
		INSERT into task (teacher_id, course_id, record_time)
		VALUES (#{teacherId}, #{courseId}, #{recordTime})
	</insert>

	<select id="searchEntity" resultMap="BaseResultMap">
		SELECT t.id, t.record_time, (SELECT name FROM teacher
		WHERE id = t.teacher_id) AS teacherName, (SELECT name FROM course
		WHERE id = t.course_id) AS courseName FROM task AS t
	</select>

	<select id="getEntityById" parameterType="int" resultMap="BaseResultMap">
		SELECT * FROM task WHERE id = #{id}
	</select>

	<delete id="deleteEntity" parameterType="int">
		DELETE FROM task WHERE id = #{id}
	</delete>

	<delete id="updateEntity" parameterType="com.xafdy.model.Task">
		UPDATE task SET teacher_id = #{teacherId}, course_id = #{courseId}, record_time = #{recordTime}
		WHERE id = #{id}
	</delete>

</mapper>